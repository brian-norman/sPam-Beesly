module.exports = {
	name: 'role',
	description: 'Assigns the user the role',
	args: true,
	usage: '<user> <role>',
	execute(message, args) {
		return message.reply(`This command doesn't do anything yet... sorry!`);
	}
}